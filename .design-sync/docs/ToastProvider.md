---
category: Feedback
---
<!-- autostub: rewritten by scaffold.mjs; delete this line to hand-own -->

# ToastProvider

Part of the `Toast` family. Context provider - wrap the subtree in it so the family's parts can read shared state.

Compose it inside `Toast` - it is not meant to stand alone.
